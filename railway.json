{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c "from app import create_app, db; app=create_app(); app.app_context().push(); db.create_all()" && gunicorn run:app"
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
